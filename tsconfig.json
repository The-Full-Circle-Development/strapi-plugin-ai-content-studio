{
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": ".",
    "baseUrl": ".",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "target": "ES2021",
    "lib": ["ES2021", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "noEmit": true
  },
  "include": ["admin", "server", "package.json"],
  "//": "Test suites are INCLUDED here on purpose: jest transpiles them without typechecking (see jest.config.js), so this is the only pass that checks them — and it checks them under the same module resolution the build uses. tsconfig.build.json still excludes them from emitted output.",
  "exclude": ["node_modules", "build", "dist", ".cache", ".tmp"]
}
