/**
 * Plugin config defaults. No user-facing config keys are required — the runtime configuration
 * (provider, model, API keys) lives in the plugin store and is managed from the Settings page.
 */
export default {
  default: {},
  validator() {},
};
